# URL completa de conexión a PostgreSQL (Supabase, Render, etc.)
DATABASE_URL=postgresql://usuario:contraseña@host:puerto/nombre_db

# Puerto en el que escucha el servidor (Render lo asigna automáticamente)
PORT=3000
